- History  {TURNS()}/{TURNS_SINCE(-> a)}/{TURNS_SINCE(-> b)}/{TURNS_SINCE(-> c)}
+ (a) a
- History  {TURNS()}/{TURNS_SINCE(-> a)}/{TURNS_SINCE(-> b)}/{TURNS_SINCE(-> c)}
+ (b)b
- History  {TURNS()}/{TURNS_SINCE(-> a)}/{TURNS_SINCE(-> b)}/{TURNS_SINCE(-> c)}
+ (c)c
History  {TURNS()}/{TURNS_SINCE(-> a)}/{TURNS_SINCE(-> b)}/{TURNS_SINCE(-> c)}
-> END
